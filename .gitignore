/Info.txt
/Abbott Test Cases/
# Import-problem case bundles (docs/import-cases.md): a fixture is installed with scripts/import-case.js add,
# which copies only case.json and the parser input. The zip also carries page images, which stay out.
import-case-*.zip
mileage-backend/.wrangler/
node_modules/

# Claude Code: session worktrees and per-machine settings stay local. The shared hook config is
# tracked so that sessions in a worktree checkout get the test gate too.
/.claude/*
!/.claude/settings.json
!/.claude/hooks/

# Xcode per-user state for the iPad app (iPad_APP/)
xcuserdata/
*.xcuserstate
iPad_APP/dist/
