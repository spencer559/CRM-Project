{
  "name": "crm-project",
  "private": true,
  "license": "UNLICENSED",
  "description": "Clinic CRM toolkit — static pages plus the .crmdb container engine in site/src/ and site/vendor/. No build step and no runtime dependencies; package.json exists only to give the test suite one entrypoint.",
  "scripts": {
    "test": "node tests/run.js"
  }
}
